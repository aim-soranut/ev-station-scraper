# Copy to secrets.toml (local) or paste into the Streamlit Community Cloud
# "Secrets" UI. Use the ANON key (read-only via RLS) -- never the service key.
SUPABASE_URL = "https://YOUR-PROJECT.supabase.co"
SUPABASE_ANON_KEY = "your-anon-public-key"
